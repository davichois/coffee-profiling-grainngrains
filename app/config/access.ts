// ─── Global access gate ───────────────────────────────────────────────────────
// Flip this to `false` to open the app. When `true`, every route shows the
// "acceso bloqueado" screen instead of the flavor wheel.
export const ACCESS_BLOCKED = false;

// Where the "buy coffee" call-to-action points.
export const SHOP_URL = "https://grainngrains.org";
